{
  "name": "scheduled-chat-health-check",
  "version": "1.0.0",
  "private": true,
  "description": "定时调用 Chat Completions 接口并在失败时发送邮件告警",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.5"
  }
}
